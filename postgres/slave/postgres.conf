### Slave/standby settings

# Allow queries on standby
hot_standby = on

# Replication feedback can reduce conflicts; safe default for many setups
# (optional, comment out if you don't need it)
hot_standby_feedback = on

